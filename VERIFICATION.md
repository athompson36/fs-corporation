# Verification record

Created 2026-09-01 on Python 3.12.13 for the 0.2.0 ZIP. Updated 2026-09-01 for 0.3.4 isolated workers (this workspace ran the unittest suite on Python 3.14.3 in `.venv`).

- 67 unit tests passed, including concurrent budget enforcement, restart persistence, loopback API auth, delegation revoke, mock ChatDev contract, GitHub denials, impact briefs, headquarters replay, period budgets, hardware skill gaps, Quality Control inspection, Human Resources training, employee hire/training files, performance trends, and subprocess-isolated worker dispatch.
- JSON configurations parsed and required documentation/local Markdown links checked.
- Governance example demonstrated denial before approval and success after approval.
- Model selection example selected an enabled compatible mock profile.
- Demo state: one accepted synthetic project, two virtual rooms, 150 simulated cost cents, one synthetic signal, valid audit chain. Repeated demo/status retained this state without duplicate growth.

Not tested or implemented: real provider inference, live ChatDev workflow execution, GitHub writes, feed retrieval, billing, container worker image, remote GitHub CI, or isometric headquarters art. The upstream commit and SDK signature were inspected; that is not end-to-end adapter verification.

To repeat checks, see docs/15-testing.md. Install service extras with `pip install -e .`. To rebuild the ZIP from source, run `python3 scripts/package.py`; output goes to `dist/`.

Version 0.2.0 added the Master Consultant heuristic scanner. Version 0.3.0 adds the loopback control service. Version 0.3.1 adds hardware skill gaps and fail-closed learning. Version 0.3.2 adds Quality Control inspection before acceptance and Human Resources training oversight. Version 0.3.3 adds hire records, recurring documented training, performance goals/reviews and trends. Version 0.3.4 adds subprocess-isolated workers with a parent-mediated gateway. The scan is heuristic and is not a comprehensive audit. No live AI review, documentation crawl, or automatic remediation was tested or enabled.
