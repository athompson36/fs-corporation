# P0.2 M10 ops + LearningAdapter.fetch Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans (or subagent-driven-development) to implement task-by-task.

**Goal:** Close remaining M10 product gaps (idempotency prune, model/benchmark read path + fixtures, companion `window.prompt` forms) and implement fail-closed `LearningAdapter.fetch` against an approved HTTPS allowlist. Update roadmap/handoff honesty.

**Architecture:** Small helpers for prune and allowlisted fetch; thin `Company` list methods + FastAPI GET routes for models/benchmarks; companion forms replace prompts; fixtures under `config/`.

**Tech Stack:** Python 3.12+, FastAPI, SQLite/`Company`, unittest, companion React/TypeScript.

## File map

| File | Responsibility |
|---|---|
| Create `company/idempotency_prune.py` | Retention default (7d), prune SQL helper |
| Modify `company/core.py` | `prune_idempotency`, `list_model_profiles`, `list_benchmark_results` |
| Modify `company/service.py` | Optional admin prune route; GET model-profiles + benchmarks |
| Create `config/benchmarks.example.json` | Deterministic role fixtures |
| Create `config/learning-sources.example.json` | Approved HTTPS URL prefixes |
| Modify `company/adapters.py` | `LearningAdapter.fetch` allowlist + httpx |
| Modify `companion/src/App.tsx` | Forms for respond / escalate / enroll |
| Create `tests/test_m10_ops.py` | Prune, list APIs, fixtures, fetch allowlist |
| Modify `tests/test_companion_api.py` | Source assertions for prompt-free forms |
| Modify docs: roadmap, handoff, 15-testing, 20-hardware-skills, 16-api-contract |

---

### Task 1: Idempotency retention prune (M10-01)

**Files:** `company/idempotency_prune.py`, `company/core.py`, `company/service.py`, `tests/test_m10_ops.py`

- Default retention: `FS_CORP_IDEMPOTENCY_RETENTION_DAYS` or `7`.
- `Company.prune_idempotency_keys(actor=None, *, older_than_days=None) -> {"deleted": N}` — deletes rows with `created_at` older than cutoff; CEO or system (None for internal/startup optional — prefer CEO-scoped API + callable from tests without actor for unit helper).
- Prefer: module function `prune_command_idempotency(db, *, older_than_days, now_iso)` + `Company.prune_idempotency_keys(actor, older_than_days=None)` with `_ceo_or_admin_companion`.
- API: `POST /api/v1/ops/idempotency/prune` payload `{older_than_days?}` scope `company.pause` (owner/admin).
- Test: insert old + new keys; prune; only old gone; retention documented in docstring + roadmap checkbox.

- [ ] Step 1: Failing tests
- [ ] Step 2: Implement
- [ ] Step 3: Pass + commit

---

### Task 2: Model profiles + benchmarks read path + fixtures (M10-03)

**Files:** `company/core.py`, `company/service.py`, `config/benchmarks.example.json`, `tests/test_m10_ops.py`

- `list_model_profiles()` from `model_profiles` table (id, enabled, body parsed).
- `list_benchmark_results(role=None)` from `benchmark_results`.
- `seed_benchmarks(path)` loads fixtures via `record_benchmark`.
- GET `/api/v1/model-profiles` and GET `/api/v1/benchmarks` — `company.read`.
- Fixture file: roles like `reviewer`, `creator` with mock-text metrics (no vendor claims).
- Test: seed → list returns rows; HTTP 200.

- [ ] Step 1–3: TDD + commit

---

### Task 3: Companion replace window.prompt (M10-04 ops forms)

**Files:** `companion/src/App.tsx`, `tests/test_companion_api.py`

- Owner inbox respond: inline textarea + submit per request (or shared form).
- Escalate: form fields department / subject / body.
- Enroll project: form id + brief (not prompts).
- Assert `window.prompt` absent from App.tsx (or only remaining none).
- `npm run build` + source tests.

- [ ] Step 1–3: TDD + commit

---

### Task 4: LearningAdapter.fetch allowlist

**Files:** `config/learning-sources.example.json`, `company/adapters.py`, `tests/test_m10_ops.py`, `docs/20-hardware-skills.md`

- Allowlist file: `{ "schema_version": 1, "url_prefixes": ["https://docs.espressif.com/"] }`.
- `fetch(url)`: HTTPS only; must match a prefix; GET with timeout; return `{url, title, summary, fetched_at, status_code}` from HTML title + truncated text (or plain); else `PermissionError` / `NotImplementedError` when no allowlist file configured — **fail closed**: missing allowlist file → NotImplementedError; URL not matching → PermissionError.
- Env override path: `FS_CORP_LEARNING_SOURCES_FILE`.
- Test: mock httpx; allowlisted succeeds; non-allowlisted raises; missing file raises NIE.

- [ ] Step 1–3: TDD + commit

---

### Task 5: Docs hygiene (M10-05 slice)

**Files:** `docs/14-roadmap.md`, `docs/18-handoff.md`, `docs/15-testing.md`, `docs/16-api-contract.md`, `docs/20-hardware-skills.md`

- Check off completed M10 items with honest version notes.
- Note remaining M10-04 chrome (version display, HQ keyboard) still open.
- Handoff next → Settings P1.

- [ ] Commit docs

## Spec coverage

| Requirement | Task |
|---|---|
| Idempotency TTL + prune | 1 |
| model_profiles / benchmark_results read | 2 |
| Role benchmark fixtures | 2 |
| Companion prompt forms | 3 |
| LearningAdapter.fetch allowlist | 4 |
| Roadmap honesty | 5 |
