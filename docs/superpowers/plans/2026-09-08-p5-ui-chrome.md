# P5 UI Chrome Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Shared cosmic-glass tokens for desk + companion; backend version in primary chrome; HQ tile keyboard access; light token polish.

**Architecture:** Single `assets/cosmic-glass-tokens.css` served at `/static/` and imported by Vite companion; desk/companion keep layout CSS local; health-driven version footers; SVG tabindex + Enter/Space.

**Tech Stack:** FastAPI StaticFiles, inline desk JS, React companion, unittest + companion build.

## Global Constraints

- Spec: `docs/superpowers/specs/2026-09-08-p5-ui-chrome-design.md` (approved).
- No invented version/HQ state; no marketing layout.
- Branch: `feature/p5-ui-chrome`. Bump `__version__` → `0.3.55`. Do not commit `local repos/service-department/`.

## File map

| Path | Role |
|---|---|
| `assets/cosmic-glass-tokens.css` | Shared tokens + primitives |
| `company/service.py` | Static mount, desk link, version, HQ keyboard, trim `:root` |
| `companion/src/styles.css` | Import tokens; trim dup colors; version strip CSS |
| `companion/src/App.tsx` | Version above tabs from `api.health()` |
| `companion/package.json` | version `0.3.55` |
| `tests/test_p5_ui_chrome.py` | Source + static route assertions |
| Docs | ADR-041, M10-04, handoff, roadmap |

---

### Task 1: Shared tokens + static mount

- [ ] Create `assets/cosmic-glass-tokens.css` with `:root` vars, `--focus-ring`, `.glass`/`.chip`/`.muted`/`.lede`, focus-visible helpers
- [ ] Mount StaticFiles in `create_app` from repo `assets/` → `/static`
- [ ] Desk `<link href="/static/cosmic-glass-tokens.css">`; remove duplicate color `:root` from inline CSS
- [ ] Companion import `../../assets/cosmic-glass-tokens.css` at top of `styles.css`; remove duplicate `:root` colors
- [ ] Tests: static 200 + `--cosmic`; desk link; companion import
- [ ] Commit `feat(p5): shared cosmic-glass tokens`

### Task 2: Version chrome + HQ keyboard

- [ ] Desk rail `#desk-version`; fill from `/api/v1/health` in `load()` / boot
- [ ] Companion muted strip above `.tabs`; state from `api.health()`
- [ ] HQ: `enableTileActivation(el, handler, label)` — tabindex, role, aria-label, click + keydown Enter/Space on rooms + workers
- [ ] CSS focus for `[data-room-id]:focus-visible`, `[data-worker-id]:focus-visible`
- [ ] Tests for markup/handlers
- [ ] Commit `feat(p5): version chrome and HQ keyboard`

### Task 3: Docs + version bump

- [ ] `__version__` 0.3.55; companion package 0.3.55; ADR-041; M10-04 `[x]`; handoff; roadmap; README matrix line
- [ ] Spec status → implemented
- [ ] Full unittest + companion build
- [ ] Commit `docs(p5): ADR-041 and M10-04 close; v0.3.55`

---

## Spec coverage

| Spec | Task |
|---|---|
| Shared tokens + static + companion import | 1 |
| Version chrome A | 2 |
| HQ keyboard | 2 |
| Light polish via tokens | 1–2 |
| ADR / M10-04 / bump | 3 |
