# Local repos

Drop git checkouts (or empty project folders) here. The CEO desk and companion list
each subdirectory as an enroll **candidate**. Enroll creates a company project with
the same id as the folder name; GitHub attach is a separate Assign GitHub step.

Override path with `FS_CORP_LOCAL_REPOS_DIR` if needed.
